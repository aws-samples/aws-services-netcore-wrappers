namespace Amazon.Sns.Wrapper.Models
{
    public enum NotificationProtocol
    {
        http,
        https,
        email,
        emailJson,
        sms,
        sqs,
        application,
        lambda,
        firehouse
    }
}
